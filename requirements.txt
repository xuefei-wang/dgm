# Pinned for deterministic installs (kcsi issue #863).
# These were previously unpinned, so every install already floated to the
# latest releases; the pins below freeze that current resolution (a consistent
# set resolved together on 2026-06-26) so a reinstall is reproducible. They are
# NOT claimed to match the published DGM baseline runs — those versions were
# never recorded. Bump deliberately.
datasets==5.0.0
anthropic[bedrock]==0.112.0
backoff==2.2.1
botocore==1.43.36
boto3==1.43.36
openai==2.44.0

# SWE-Bench
beautifulsoup4==4.15.0
chardet==7.4.3
docker==7.1.0
ghapi==1.0.14
GitPython==3.1.50
python-dotenv==1.2.2
rich==15.0.0
unidiff==0.7.5

# pytest
pytest==9.1.1
pytest-asyncio==1.4.0
async_timeout==5.0.1
